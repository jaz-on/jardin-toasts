---
alwaysApply: true
---
# Jardin Toasts - Architecture Rules

## Conventions WordPress

### Coding Standards
- **WPCS**: Follow WordPress Coding Standards (WPCS) strictly
- **PHPStan**: Level 5 minimum for static analysis
- **Indentation**: Tabs (not spaces) for PHP files
- **Line endings**: Unix-style (LF)
- **File headers**: All PHP files must include proper headers with license

### Naming Conventions

#### Functions
- **Prefix**: `jb_` for public functions (e.g., `jb_display_rating()`)
- **Private functions**: `_jb_` prefix (e.g., `_jb_parse_rss_item()`)
- **Naming**: lowercase with underscores, descriptive names
- **Examples**:
  - `jb_get_checkin_data($post_id)`
  - `jb_rating_stars($rating, $echo = true)`
  - `jb_beer_image($post_id, $size = 'medium')`

#### Classes
- **Prefix**: `JB_` for all classes (e.g., `JB_Importer`, `JB_Scraper`)
- **Naming**: PascalCase
- **File naming**: `class-{name-lowercase}.php` (e.g., `class-importer.php`)
- **Examples**:
  - `JB_RSS_Parser` → `includes/class-rss-parser.php`
  - `JB_Scraper` → `includes/class-scraper.php`
  - `JB_Image_Handler` → `includes/class-image-handler.php`

#### Constants
- **Prefix**: `JB_` (e.g., `JB_VERSION`, `JB_PLUGIN_DIR`)
- **Naming**: UPPERCASE with underscores

#### Options (WordPress)
- **Prefix**: `jb_` (e.g., `jb_last_checkin_date`, `jb_rating_rules`)
- **Naming**: lowercase with underscores

#### Meta Keys
- **Prefix**: `_jb_` (e.g., `_jb_checkin_id`, `_jb_rating_raw`)
- **Naming**: lowercase with underscores
- **Note**: Leading underscore makes meta keys "hidden" in WordPress admin

#### Transients
- **Prefix**: `jb_` (e.g., `jb_new_terms_notice`, `jb_global_stats`)

### Text Domain
- **Text domain**: `jardin-toasts` (with hyphen)
- **Usage**: All user-facing strings must be translatable
- **Examples**:
  ```php
  __('Beer Check-ins', 'jardin-toasts')
  _e('Import Historical Check-ins', 'jardin-toasts')
  _n('%s check-in', '%s check-ins', $count, 'jardin-toasts')
  esc_html__('Rating System', 'jardin-toasts')
  ```

## Security Standards

### Data Sanitization
- **Text fields**: `sanitize_text_field()`
- **Rich text**: `wp_kses_post()` or `wp_kses()` with allowed tags
- **URLs**: `esc_url_raw()` for storage, `esc_url()` for output
- **Numbers**: `absint()` for integers, `floatval()` for floats
- **Emails**: `sanitize_email()`

### Data Escaping
- **HTML output**: `esc_html()`
- **Attributes**: `esc_attr()`
- **URLs**: `esc_url()`
- **JavaScript**: `wp_json_encode()` or `esc_js()`

### Nonces
- **All forms**: Include nonces with `wp_nonce_field()` or `wp_create_nonce()`
- **AJAX requests**: Verify with `check_ajax_referer()`
- **Admin actions**: Verify with `check_admin_referer()`
- **Naming**: `jb_{action}_nonce` (e.g., `jb_import_nonce`)

### Capability Checks
- **Admin pages**: `current_user_can('manage_options')`
- **Edit posts**: `current_user_can('edit_post', $post_id)`
- **Delete posts**: `current_user_can('delete_post', $post_id)`

### Input Validation
- **URLs**: Validate with `filter_var($url, FILTER_VALIDATE_URL)`
- **Untappd URLs**: Verify domain contains `untappd.com`
- **RSS feeds**: Validate RSS format
- **File uploads**: Validate file types and sizes

## Internationalization (i18n)

### Requirements
- **All user-facing strings**: Must be translatable
- **Text domain**: Always use `'jardin-toasts'`
- **Context**: Use `_x()` when context is needed
- **Plurals**: Use `_n()` for plural forms

### Examples
```php
// Simple string
echo esc_html__('Beer Check-ins', 'jardin-toasts');

// String with output
_e('Import Historical Check-ins', 'jardin-toasts');

// String with context
_x('Brewery', 'taxonomy singular name', 'jardin-toasts');

// Plural forms
printf(
    _n('%s check-in', '%s check-ins', $count, 'jardin-toasts'),
    number_format_i18n($count)
);

// Escaped output
echo esc_html__('Rating System', 'jardin-toasts');
echo esc_attr__('Beer photo', 'jardin-toasts');
```

### Loading Text Domain
```php
add_action('plugins_loaded', 'jb_load_textdomain');

function jb_load_textdomain() {
    load_plugin_textdomain(
        'jardin-toasts',
        false,
        dirname(plugin_basename(__FILE__)) . '/languages/'
    );
}
```

## File Structure

### Main Plugin File
- **Location**: `jardin-toasts.php` (root)
- **Headers**: Must include WordPress plugin headers
- **Activation/Deactivation**: Use separate classes

### Includes Directory
- **Location**: `includes/`
- **Files**: `class-{name}.php`
- **Purpose**: Core functionality classes

### Admin Directory
- **Location**: `admin/`
- **Structure**:
  - `class-admin.php` - Main admin class
  - `views/` - Admin page templates
  - `assets/` - CSS and JS for admin
  - `partials/` - Reusable admin components

### Public Directory
- **Location**: `public/`
- **Structure**:
  - `class-public.php` - Frontend hooks
  - `templates/` - Default templates (overridable)
  - `assets/` - CSS and JS for frontend
  - `partials/` - Reusable frontend components

## WordPress Integration

### Hooks Priority
- **Actions**: Use appropriate priority (default 10)
- **Filters**: Use appropriate priority (default 10)
- **Late hooks**: Use priority 999+ for overrides

### Database Queries
- **Use WP_Query**: Never use direct SQL for posts
- **Meta queries**: Use `meta_query` parameter
- **Taxonomy queries**: Use `tax_query` parameter
- **Performance**: Always set `no_found_rows => false` for pagination

### Caching
- **Transients**: For temporary data (stats, etc.)
- **Object cache**: Use `wp_cache_get()` and `wp_cache_set()`
- **Cache keys**: Prefix with `jb_` (e.g., `jb_global_stats`)

## Error Handling

### WordPress Errors
- **Use WP_Error**: For function return values
- **Logging**: Use `error_log()` with plugin prefix
- **User feedback**: Use admin notices for user-facing errors

### Example
```php
function jb_import_checkin($data) {
    if (empty($data['beer_name'])) {
        return new WP_Error(
            'missing_beer_name',
            __('Beer name is required', 'jardin-toasts')
        );
    }
    
    // Process...
    
    if ($error) {
        error_log('Jardin Toasts: Import failed - ' . $error_message);
        return new WP_Error('import_failed', $error_message);
    }
    
    return $post_id;
}
```

## Performance

### Optimization Rules
- **Lazy loading**: Images use `loading="lazy"` attribute
- **Pagination**: Always implement pagination for lists
- **Database indexes**: Document required indexes
- **Query optimization**: Use `update_post_meta_cache` and `update_post_term_cache`
- **Transients**: Cache expensive operations

### Example Query
Use this shape for **custom** `WP_Query` loops (e.g. widgets or shortcodes). Theme archive templates for `beer_checkin` normally use the **main query**; do not set `no_found_rows` there.

```php
$args = [
    'post_type' => 'beer_checkin',
    'posts_per_page' => 24,
    'no_found_rows' => false, // Required for pagination
    'update_post_meta_cache' => true,
    'update_post_term_cache' => true,
];
```

## Documentation

### Code Comments
- **PHPDoc**: All public functions and classes
- **Inline comments**: Explain complex logic
- **File headers**: Include purpose and author

### PHPDoc Example
```php
/**
 * Display rating with stars and optional label
 * 
 * @param int  $post_id    Post ID
 * @param bool $show_label Show custom label
 * @param bool $show_raw   Show original rating in tooltip
 * @return string HTML output
 */
function jb_display_rating($post_id, $show_label = true, $show_raw = true) {
    // Implementation...
}
```

## Dependencies

### Composer
- **Production**: PHP only (no Symfony / HTML crawler — check-ins come from RSS + user-provided CSV export).

### WordPress
- **SimplePie**: RSS parsing (built-in)
- **WP_HTTP** (`wp_remote_get`, etc.): Images and integrations when needed — not used to scrape Untappd check-in HTML.

## Version Compatibility

### WordPress
- **Minimum**: 6.0
- **Tested up to**: 7.0
- **Future**: Keep compatibility with latest

### PHP
- **Minimum**: 8.2
- **Recommended**: 8.3+
- **Features**: Use modern PHP features (typed properties, etc.)

## Git Workflow

### Branches
- **main**: Default branch; production-ready / reference code
- **dev**: Development integration; Git Updater on dev sites targets this branch
- **feature/**: Feature branches (from `dev` unless hotfix from `main`)

### Commits
- **Format**: Conventional commits preferred
- **Messages**: Clear and descriptive
- **Scope**: One logical change per commit

### Version numbers
- Do not bump the plugin `Version` header or `JARDIN_TOASTS_VERSION` unless the maintainer explicitly authorizes it.

## Testing

### Requirements
- **PHPUnit**: For unit tests
- **Coverage**: Aim for 80%+ for critical paths
- **Integration tests**: For import/sync processes

## Summary

- **Prefixes**: `jb_` (functions), `JB_` (classes), `_jb_` (meta keys)
- **Text domain**: `jardin-toasts`
- **Security**: Always sanitize input, escape output, use nonces
- **i18n**: All user-facing strings translatable
- **Standards**: WordPress Coding Standards (WPCS)
- **Performance**: Cache, optimize queries, lazy loading
- **Documentation**: PHPDoc for all public APIs
